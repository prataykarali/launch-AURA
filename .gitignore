# Rust build artifacts — 3GB+, never commit
candle_LNN/aura_lnn/target/

# Flutter build artifacts
aura_notebook/build/
aura_notebook/.dart_tool/
aura_notebook/.flutter-plugins
aura_notebook/.flutter-plugins-dependencies

# Model weights — keep local only, never push
*.gguf
candle_LNN/aura_lnn/models/

# IDE
.vscode/
.idea/
*.gguf
*.kvcache
*.pos
target/
.fastembed_cache/
*.gguf
.fastembed_cache/
.aider*

# Model weights — keep local only
*.onnx
*.safetensors

# Memory databases — runtime state, not source
*.db
*.db-wal
*.db-shm

# Android system shared lib — not ours
libc++_shared.so

# Build reports
aura_notebook/build/reports/
aura_notebook/linux/bundle/

# Root-level .so copies — use linux_libs/ instead
aura_notebook/libaura_lnn.so
