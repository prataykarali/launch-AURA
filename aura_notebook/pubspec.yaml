name: aura_notebook
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.8

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.5
  flutter_rust_bridge: 2.12.0
  flutter_animate: ^4.5.0
  glassmorphism: ^3.0.0        # glass pill shell
  sleek_circular_slider: ^2.0.0
  ffi: ^2.2.0
  build_runner: ^2.13.1
  wakelock_plus: ^1.2.8
  path_provider: ^2.1.5
  window_manager: ^0.5.1
  desktop_multi_window: ^0.3.0
  flutter_overlay_window: ^0.5.0
  screen_retriever: ^0.2.0
  sqflite: ^2.4.1
  sqflite_common_ffi: ^2.3.6
  sqlite3_flutter_libs: ^0.5.24
#  atmospheric_particles: ^1.0.0
#  tray_manager: ^0.2.0
  atmospheric_particles: ^1.0.0
  speech_to_text: ^7.0.0
  permission_handler: ^11.3.1
  audioplayers: ^6.7.1
  crypto: ^3.0.3

  
  
# flutter_overlay_window 0.5.0 crashes on Android 14+ (API 34+) when the app
# targets SDK 34+: OverlayService calls the 2-arg startForeground(), which throws
# SecurityException "requires android.permission.FOREGROUND_SERVICE" for a
# service declared with foregroundServiceType="specialUse". The plugin needs the
# 3-arg overload (passing FOREGROUND_SERVICE_TYPE_SPECIAL_USE) on API 34+. This
# local patched copy applies that fix; upstream 0.5.0 hasn't shipped it. The
# duplicate `flutter_overlay_window` entry above (the versioned dep) is kept so
# `pub get` still resolves normally — this override silently supersedes it.
dependency_overrides:
  flutter_overlay_window:
    path: plugins/flutter_overlay_window

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - Assets/images/
    # NOTE: The GGUF chat model is deliberately NOT bundled here.
    # Baking it into Flutter assets produced a ~982 MB APK that Android 16
    # SIGKILL'd at process start (handleProcessStartOrKillTimeoutLocked) before
    # main()/Dart ever ran — so the bar, TTS and loader all silently failed.
    # Desktop resolves it from the bundle/source asset dir at runtime
    # (loading_screen.dart); Android downloads it on first run from HuggingFace.
    - assets/tokenizer.json
    - assets/whisper_server.py
    # NOTE: Piper TTS voices (134 MB) are Linux-desktop-only (Android uses the
    # system TextToSpeech engine). Excluded from the APK to keep install size
    # down. Desktop resolves them from the source assets dir at runtime.
    # - assets/piper/
    # - assets/piper/espeak-ng-data/
    # - assets/piper/espeak-ng-data/lang/
    # - assets/piper/espeak-ng-data/voices/
    # - assets/piper/espeak-ng-data/voices/!v/
    # NOTE: Embedder (110 MB model_quantized.onnx) is also excluded — downloaded
    # on first run on Android. Desktop resolves from the source assets dir.
    # - assets/embedder/

  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "Assets/images/AURA.png" # Make sure this points to your square image
  min_sdk_android: 21
  # This tells Android how to crop it properly without stretching:
  adaptive_icon_background: "#E8C8B5" # Put the hex color of your image's background here
  adaptive_icon_foreground: "Assets/images/AURA.png"
